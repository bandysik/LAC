package lac.feature.main.app.settings.city

import lac.feature.main.data.Repository

class CityDialogPresenter(private val view: CityDialogContract.Presenter,
                          private val repository: Repository) : CityDialogContract.Presenter {

    override fun start() {
    }

    override fun stop() {
    }
}